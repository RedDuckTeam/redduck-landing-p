import { ReadyToBuild } from '~/components/common';

export function Ready() {
  return (
    <ReadyToBuild
      ctaLabel="Book a Free ZK Architecture Call"
      blurb="Most ZK projects fail at the architecture stage — proving the wrong thing, or proving it in a way that's too expensive to verify on-chain. We get the proof system and circuit design right before implementation starts."
    />
  );
}

export default Ready;
