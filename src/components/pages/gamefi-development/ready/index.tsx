import { ReadyToBuild } from '~/components/common';

export function Ready() {
  return (
    <ReadyToBuild
      ctaLabel="Book a Free Architecture Call"
      blurb="Most GameFi projects die when tokenomics and game design are treated as separate problems. We get the economy and the mechanics right together — before a single contract ships."
    />
  );
}

export default Ready;
